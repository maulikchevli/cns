pub fn encrypt(plain_text: &String, depth: &usize) -> String {
    let mut cipher_text = String::from("Cipher");

    cipher_text
}

pub fn decrypt(cipher_text: &String, depth: &usize) -> String {
    let mut plain_text = String::from("Plain");

    plain_text
}
